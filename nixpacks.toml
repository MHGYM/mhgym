[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "cd client && npm install"]

[phases.build]
cmds = ["cd client && npm run build"]

[variables]
NODE_ENV = "production"

[start]
cmd = "node migrations/014_shared_email.js; node server.js"
